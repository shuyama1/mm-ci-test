steps:
- name: bash
  script: |
    #!/usr/bin/env bash
    echo "Second Job"
    echo $BRANCH_NAME
    echo $COMMIT_SHA
    echo $_VAR_1
    echo $_BUILD_STATUS
    echo $_BUILD_TRIGGER_ID
    echo "Done"
